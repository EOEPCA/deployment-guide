#!/usr/bin/env bash

ORIG_DIR="$(pwd)"
cd "$(dirname "$0")"
BIN_DIR="$(pwd)"

onExit() {
  cd "${ORIG_DIR}"
}
trap onExit EXIT

ACTION="${@:-apply}"

echo -e "\nCreate letsencrypt cluster issuer..."
kubectl ${ACTION} -f yaml
