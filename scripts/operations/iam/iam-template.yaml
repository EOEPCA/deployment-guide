---
apiVersion: v1
kind: Secret
metadata:
  name: {{ getenv "GRAFANA_CLIENT_ID" }}-keycloak-client
  namespace: iam-management
stringData:
  client_secret: {{ getenv "GRAFANA_CLIENT_SECRET" }}
---
# Grafana client (monitoring)
apiVersion: openidclient.keycloak.m.crossplane.io/v1alpha1
kind: Client
metadata:
  name: {{ getenv "GRAFANA_CLIENT_ID" }}
  namespace: iam-management
spec:
  providerConfigRef:
    name: keycloak-provider-config
    kind: ProviderConfig
  forProvider:
    realmId: {{ getenv "REALM" }}
    name: {{ getenv "GRAFANA_CLIENT_ID" }}
    accessType: CONFIDENTIAL
    clientId: {{ getenv "GRAFANA_CLIENT_ID" }}
    enabled: true
    standardFlowEnabled: true
    implicitFlowEnabled: false
    directAccessGrantsEnabled: false
    fullScopeAllowed: true
    serviceAccountsEnabled: false
    useRefreshTokens: true
    rootUrl: {{ getenv "HTTP_SCHEME" }}://monitoring.{{ getenv "INGRESS_HOST" }}
    validRedirectUris:
      - {{ getenv "HTTP_SCHEME" }}://monitoring.{{ getenv "INGRESS_HOST" }}/login/generic_oauth
    validPostLogoutRedirectUris:
      - {{ getenv "HTTP_SCHEME" }}://monitoring.{{ getenv "INGRESS_HOST" }}/
      - {{ getenv "HTTP_SCHEME" }}://monitoring.{{ getenv "INGRESS_HOST" }}/login
    webOrigins:
      - {{ getenv "HTTP_SCHEME" }}://monitoring.{{ getenv "INGRESS_HOST" }}
    clientSecretSecretRef:
      name: {{ getenv "GRAFANA_CLIENT_ID" }}-keycloak-client
      key: client_secret
---
apiVersion: openidclient.keycloak.m.crossplane.io/v1alpha1
kind: ClientOptionalScopes
metadata:
  name: {{ getenv "GRAFANA_CLIENT_ID" }}-optional-scopes
  namespace: iam-management
spec:
  providerConfigRef:
    name: keycloak-provider-config
    kind: ProviderConfig
  forProvider:
    realmId: {{ getenv "REALM" }}
    clientIdRef:
      name: {{ getenv "GRAFANA_CLIENT_ID" }}
      policy:
        resolution: Required
    optionalScopes:
      - offline_access
---
apiVersion: role.keycloak.m.crossplane.io/v1alpha1
kind: Role
metadata:
  name: grafana-admin
  namespace: iam-management
spec:
  providerConfigRef:
    name: keycloak-provider-config
    kind: ProviderConfig
  forProvider:
    realmId: {{ getenv "REALM" }}
    name: grafana_admin
    description: Admin access for monitoring stack
    clientIdRef:
      name: {{ getenv "GRAFANA_CLIENT_ID" }}
      policy:
        resolution: Required
---
apiVersion: role.keycloak.m.crossplane.io/v1alpha1
kind: Role
metadata:
  name: grafana-editor
  namespace: iam-management
spec:
  providerConfigRef:
    name: keycloak-provider-config
    kind: ProviderConfig
  forProvider:
    realmId: {{ getenv "REALM" }}
    name: grafana_editor
    description: Editor access for monitoring stack
    clientIdRef:
      name: {{ getenv "GRAFANA_CLIENT_ID" }}
      policy:
        resolution: Required
---
apiVersion: role.keycloak.m.crossplane.io/v1alpha1
kind: Role
metadata:
  name: grafana-viewer
  namespace: iam-management
spec:
  providerConfigRef:
    name: keycloak-provider-config
    kind: ProviderConfig
  forProvider:
    realmId: {{ getenv "REALM" }}
    name: grafana_viewer
    description: Viewer access for monitoring stack
    clientIdRef:
      name: {{ getenv "GRAFANA_CLIENT_ID" }}
      policy:
        resolution: Required
---
apiVersion: group.keycloak.m.crossplane.io/v1alpha1
kind: Group
metadata:
  name: monitoring-admin
  namespace: iam-management
spec:
  providerConfigRef:
    name: keycloak-provider-config
    kind: ProviderConfig
  forProvider:
    realmId: {{ getenv "REALM" }}
    name: monitoring-admin
---
apiVersion: group.keycloak.m.crossplane.io/v1alpha1
kind: Roles
metadata:
  name: monitoring-admin
  namespace: iam-management
spec:
  providerConfigRef:
    name: keycloak-provider-config
    kind: ProviderConfig
  forProvider:
    realmId: {{ getenv "REALM" }}
    groupIdRef:
      name: monitoring-admin
      policy:
        resolution: Required
    roleIdsRefs:
      - name: grafana-admin
        policy:
          resolution: Required
---
apiVersion: group.keycloak.m.crossplane.io/v1alpha1
kind: Memberships
metadata:
  name: monitoring-admin-members
  namespace: iam-management
spec:
  providerConfigRef:
    name: keycloak-provider-config
    kind: ProviderConfig
  forProvider:
    realmId: {{ getenv "REALM" }}
    groupIdRef:
      name: monitoring-admin
      policy:
        resolution: Required
    members:
      - {{ getenv "KEYCLOAK_TEST_ADMIN" }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ getenv "KEEP_CLIENT_ID" }}-keycloak-client
  namespace: iam-management
stringData:
  client_secret: {{ getenv "KEEP_CLIENT_SECRET" }}
---
# Keep client (alerting)
apiVersion: openidclient.keycloak.m.crossplane.io/v1alpha1
kind: Client
metadata:
  name: {{ getenv "KEEP_CLIENT_ID" }}
  namespace: iam-management
spec:
  providerConfigRef:
    name: keycloak-provider-config
    kind: ProviderConfig
  forProvider:
    realmId: {{ getenv "REALM" }}
    name: {{ getenv "KEEP_CLIENT_ID" }}
    accessType: CONFIDENTIAL
    clientId: {{ getenv "KEEP_CLIENT_ID" }}
    enabled: true
    standardFlowEnabled: true
    implicitFlowEnabled: false
    directAccessGrantsEnabled: false
    fullScopeAllowed: true
    serviceAccountsEnabled: false
    useRefreshTokens: true
    rootUrl: {{ getenv "HTTP_SCHEME" }}://alerting.{{ getenv "INGRESS_HOST" }}
    validRedirectUris:
      - {{ getenv "HTTP_SCHEME" }}://alerting.{{ getenv "INGRESS_HOST" }}/oauth2/callback
    validPostLogoutRedirectUris:
      - {{ getenv "HTTP_SCHEME" }}://alerting.{{ getenv "INGRESS_HOST" }}/
      - {{ getenv "HTTP_SCHEME" }}://alerting.{{ getenv "INGRESS_HOST" }}/oauth2/*
    webOrigins:
      - {{ getenv "HTTP_SCHEME" }}://alerting.{{ getenv "INGRESS_HOST" }}
    clientSecretSecretRef:
      name: {{ getenv "KEEP_CLIENT_ID" }}-keycloak-client
      key: client_secret
---
apiVersion: openidclient.keycloak.m.crossplane.io/v1alpha1
kind: ClientOptionalScopes
metadata:
  name: {{ getenv "KEEP_CLIENT_ID" }}-optional-scopes
  namespace: iam-management
spec:
  providerConfigRef:
    name: keycloak-provider-config
    kind: ProviderConfig
  forProvider:
    realmId: {{ getenv "REALM" }}
    clientIdRef:
      name: {{ getenv "KEEP_CLIENT_ID" }}
      policy:
        resolution: Required
    optionalScopes:
      - offline_access
---
apiVersion: role.keycloak.m.crossplane.io/v1alpha1
kind: Role
metadata:
  name: alerting-keep-admin
  namespace: iam-management
spec:
  providerConfigRef:
    name: keycloak-provider-config
    kind: ProviderConfig
  forProvider:
    realmId: {{ getenv "REALM" }}
    name: keep_admin
    description: Admin access for alerting stack
    clientIdRef:
      name: {{ getenv "KEEP_CLIENT_ID" }}
      policy:
        resolution: Required
---
apiVersion: role.keycloak.m.crossplane.io/v1alpha1
kind: Role
metadata:
  name: alerting-keep-noc
  namespace: iam-management
spec:
  providerConfigRef:
    name: keycloak-provider-config
    kind: ProviderConfig
  forProvider:
    realmId: {{ getenv "REALM" }}
    name: keep_noc
    description: NOC access for alerting stack
    clientIdRef:
      name: {{ getenv "KEEP_CLIENT_ID" }}
      policy:
        resolution: Required
---
apiVersion: role.keycloak.m.crossplane.io/v1alpha1
kind: Role
metadata:
  name: alerting-keep-webhook
  namespace: iam-management
spec:
  providerConfigRef:
    name: keycloak-provider-config
    kind: ProviderConfig
  forProvider:
    realmId: {{ getenv "REALM" }}
    name: keep_webhook
    description: Webhook ingestion access for alerting stack
    clientIdRef:
      name: {{ getenv "KEEP_CLIENT_ID" }}
      policy:
        resolution: Required
---
apiVersion: group.keycloak.m.crossplane.io/v1alpha1
kind: Group
metadata:
  name: alerting-admin
  namespace: iam-management
spec:
  providerConfigRef:
    name: keycloak-provider-config
    kind: ProviderConfig
  forProvider:
    realmId: {{ getenv "REALM" }}
    name: alerting-admin
---
apiVersion: group.keycloak.m.crossplane.io/v1alpha1
kind: Roles
metadata:
  name: alerting-admin
  namespace: iam-management
spec:
  providerConfigRef:
    name: keycloak-provider-config
    kind: ProviderConfig
  forProvider:
    realmId: {{ getenv "REALM" }}
    groupIdRef:
      name: alerting-admin
      policy:
        resolution: Required
    roleIdsRefs:
      - name: alerting-keep-admin
        policy:
          resolution: Required
---
apiVersion: group.keycloak.m.crossplane.io/v1alpha1
kind: Memberships
metadata:
  name: alerting-admin-members
  namespace: iam-management
spec:
  providerConfigRef:
    name: keycloak-provider-config
    kind: ProviderConfig
  forProvider:
    realmId: {{ getenv "REALM" }}
    groupIdRef:
      name: alerting-admin
      policy:
        resolution: Required
    members:
      - {{ getenv "KEYCLOAK_TEST_ADMIN" }}
---
apiVersion: client.keycloak.m.crossplane.io/v1alpha1
kind: ProtocolMapper
metadata:
  name: alerting-client-roles-groups
  namespace: iam-management
spec:
  providerConfigRef:
    name: keycloak-provider-config
    kind: ProviderConfig
  forProvider:
    clientIdRef:
      name: {{ getenv "KEEP_CLIENT_ID" }}
      policy:
        resolution: Required
    realmId: {{ getenv "REALM" }}
    protocol: openid-connect
    protocolMapper: oidc-usermodel-client-role-mapper
    name: alerting-client-roles-groups
    config:
      "access.token.claim": "true"
      "id.token.claim": "true"
      "userinfo.token.claim": "true"
      "multivalued": "true"
      "claim.name": "groups"
      "jsonType.label": "String"
      "usermodel.clientRoleMapping.clientId": "{{ getenv "KEEP_CLIENT_ID" }}"