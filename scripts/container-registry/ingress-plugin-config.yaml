apiVersion: apisix.apache.org/v2
kind: ApisixPluginConfig
metadata:
  name: harbor-ingress
spec:
  plugins:
    - name: client-control
      enable: true
      config:
        max_body_size: 10737418240
