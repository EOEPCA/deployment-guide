apiVersion: user.keycloak.m.crossplane.io/v1alpha1
kind: User
metadata:
  name: eoepca-user
  namespace: iam-management
spec:
  providerConfigRef:
    name: keycloak-provider-config
    kind: ProviderConfig
  forProvider:
    realmId: {{ getenv "REALM" | quote }}
    username: {{ getenv "KEYCLOAK_TEST_USER" | quote }}
    firstName: {{ getenv "KEYCLOAK_TEST_USER" | quote }}
    lastName: Testuser
    email: "{{ getenv "KEYCLOAK_TEST_USER" }}@eoepca.org"
    enabled: true
    emailVerified: true
    initialPassword:
      - temporary: false
        valueSecretRef:
          name: eoepca-user
          key: eoepcauser-password
