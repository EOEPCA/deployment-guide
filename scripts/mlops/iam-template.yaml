apiVersion: v1
kind: Secret
metadata:
  name: {{ getenv "MLOPS_OIDC_CLIENT_ID" }}-keycloak-client
  namespace: iam-management
stringData:
  client_secret: {{ getenv "MLOPS_OIDC_CLIENT_SECRET" }}
---
apiVersion: openidclient.keycloak.m.crossplane.io/v1alpha1
kind: Client
metadata:
  name: {{ getenv "MLOPS_OIDC_CLIENT_ID" }}
  namespace: iam-management
spec:
  forProvider:
    realmId: {{ getenv "REALM" }}
    clientId: {{ getenv "MLOPS_OIDC_CLIENT_ID" }}
    name: Gitlab
    description: Gitlab OIDC
    enabled: true
    accessType: CONFIDENTIAL
    rootUrl: {{ getenv "HTTP_SCHEME" }}://gitlab.{{ getenv "INGRESS_HOST" }}
    baseUrl: {{ getenv "HTTP_SCHEME" }}://gitlab.{{ getenv "INGRESS_HOST" }}
    adminUrl: {{ getenv "HTTP_SCHEME" }}://gitlab.{{ getenv "INGRESS_HOST" }}
    serviceAccountsEnabled: true
    directAccessGrantsEnabled: true
    standardFlowEnabled: true
    oauth2DeviceAuthorizationGrantEnabled: true
    useRefreshTokens: true
    authorization:
      - allowRemoteResourceManagement: true
        decisionStrategy: UNANIMOUS
        keepDefaults: true
        policyEnforcementMode: ENFORCING
    validRedirectUris:
      - "/*"
    webOrigins:
      - "/*"
    clientSecretSecretRef:
      name: {{ getenv "MLOPS_OIDC_CLIENT_ID" }}-keycloak-client
      key: client_secret
  providerConfigRef:
    name: keycloak-provider-config
    kind: ProviderConfig
